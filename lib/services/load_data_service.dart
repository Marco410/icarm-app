import 'package:flutter/material.dart';

import '../share_prefs/prefs_usuario.dart';

class LoadDataService extends ChangeNotifier {
  load_data(BuildContext context) {
    final prefs = new PreferenciasUsuario();
  }
}
